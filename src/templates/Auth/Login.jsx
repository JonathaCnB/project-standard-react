import * as Styled from './styles';
import React from 'react';

const Login = () => {
  return (
    <Styled.Container>
      <Styled.Login>
        <h1>Login</h1>
      </Styled.Login>
    </Styled.Container>
  );
};

export default Login;
